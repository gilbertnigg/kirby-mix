import GLightbox from "glightbox";

const lightbox = GLightbox({
	touchNavigation: true,
	autoplayVideos: true,
	openEffect: "fade",
	closeEffect: "fade",
	descPosition: "bottom",
	effect: "fade",
	width: "80rem",
	height: "auto",
	zoomable: false,
	draggable: false,
});
