name: field.blocks.image.name
icon: image
preview: fields
wysiwyg: true
tabs:
  main:
    label: Bild
    fields:
      image:
        extends: fields/image
        width: 1/1
        layout: cards
        size: xlarge
        image:
          ratio: 16/9
          back: white
  settings:
    label: Darstellung
    fields:
      image_size:
        label: Bildgrösse
        type: toggles
        required: true
        default: lg
        grow: false
        options:
          sm: klein
          md: mittel
          lg: gross
