label: Dateiablage
icon: attachment
columns:
  - width: 1/3
    sections:
      data_files:
        headline: Dokumente
        type: files
        template: file
        info: "{{ file.description }}"
        batch: true
        sortable: false
        sortBy: name asc
        image:
          back: black
  - width: 1/3
    sections:
      data_images:
        headline: Bilder
        type: files
        template: image
        info: "{{ file.alt }}"
        batch: true
        sortable: false
        image:
          back: black
  - width: 1/3
    sections:
      data_videos:
        headline: Videos
        type: files
        template: video
        info: "{{ file.description }}"
        batch: true
        sortable: false
        sortBy: name asc
        image:
          back: black
