name: Spalten
icon: grid
preview: fields
wysiwyg: true
tabs:
  main:
    label: Inhalte
    fields:
      title:
        label: Titel
        type: text
      structure:
        label: Spalten
        type: structure
        columns:
          title: true
        fields:
          image:
            extends: fields/image
          title:
            label: Titel
            type: text
            required: true
          text:
            extends: fields/writer
  settings:
    label: Darstellung
    fields:
      background:
        label: Hintergrund
        type: toggles
        required: true
        default: light
        width: 1/4
        options:
          light: hell
          dark: dunkel
      textalign:
        label: Textausrichtung
        type: toggles
        required: true
        default: left
        width: 1/4
        options:
          left: links
          center: zentriert
      rounded:
        label: Bild
        type: toggles
        required: true
        default: square
        width: 1/4
        options:
          square: eckig
          rounded: rund
      columns:
        label: Spalten
        type: toggles
        width: 1/4
        required: true
        default: 4
        options:
          - 2
          - 3
          - 4
